# Resource-only fragment: logback.xml must sit at the root of the built jar so it
# lands on the host bundle's classpath, which is where logback looks for it.
bin.includes = META-INF/,\
               logback.xml
src.includes = pom.xml
